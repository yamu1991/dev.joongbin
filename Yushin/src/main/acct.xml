SELECT ACCT_NO
     , INO_DV_CD
     , TRSC_AMT
     , TRSC_DT
     , TRSC_SEQ
  FROM TB_ACCT_PTCL
 WHERE 1=1
   AND ACCT_NO = :ACCT_NO
;


INSERT INTO TB_ACCT_PTCL
(ACCT_NO, INO_DV_CD, TRSC_AMT, TRSC_DT, TRSC_SEQ)
VALUES
(:ACCT_NO, :INO_DV_CD, :TRSC_AMT, SYSDATE, :TRSC_SEQ)
;
